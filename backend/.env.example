# Copy to .env — matches the credentials in the root docker-compose.yml
DATABASE_URL="postgresql://quiz:quiz@localhost:5432/quiz_builder?schema=public"

# API port; the frontend expects http://localhost:4000
PORT=4000

# Origin allowed by CORS — the Next.js dev server
CORS_ORIGIN=http://localhost:3000
